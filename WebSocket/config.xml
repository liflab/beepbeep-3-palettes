<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!--
  Configuration file for AntRun.
  
  Use this file to override the default properties contained
  in build.xml. For example, to override the build.jar.filename
  property (which sets the name of the compiled jar file), add
  an element
  
  <jar>
    <filename>foo</filename>
  </jar>
  
  inside the <build> element.
  
  **Caveat emptor:** in some cases, changing this file and running ant
  again will result in Ant using a cached version of the old file! Run
  "ant dummy" to make sure that the correct data is being read. Otherwise
  you might need to run "ant clean" and start over.
-->
<build>
  <!-- Uncomment any of these if you want to override the build
       script's default values -->

  <!-- The project's name -->
  <name>WebSocket palette for BeepBeep 3</name>
  
  <!-- The project's version number -->
  <version>0.2</version>
  
  <!-- The project's author. Currently this only
  shows up in the footer of the Javadoc documentation. -->
  <author>Sylvain Hall&#233;</author>
  
  <!-- The project's main class -->
  <mainclass>ca.uqac.lif.cep.io.websocket.Main</mainclass>
  
  <!-- The location of rt.jar -->
  <rtlocation>../rt.jar</rtlocation>
  
  <!-- Installation folders -->
  <srcdir>src</srcdir>
  <libdir>../lib</libdir>
  <bindir>bin</bindir>
  <docdir>doc</docdir>
  <test>
    <srcdir>src</srcdir>
    <bindir>bin</bindir>
    <filenamepattern>**/*Test.java</filenamepattern>
  </test>
  
  <!-- Output jar -->
  <jar>
    <filename>../jars/websocket.jar</filename>
    <withdoc>false</withdoc>
  </jar>
  
  <!--
    Write JAR dependencies here.
  -->
  <dependencies>
  	<dependency>
      <!--
      BeepBeep
      -->
      <name>BeepBeep</name>
      <classname>ca.uqac.lif.cep.Processor</classname>
      <files>
      	<zip>https://github.com/liflab/beepbeep-3/releases/download/v0.10.3-alpha/beepbeep-3-0.10.3-alpha.zip</zip>
      </files>
      <bundle>false</bundle>
    </dependency>
    <dependency>
    <!-- java_websocket. Used to implement the websocket -->
      <name>json-lif</name>
      <classname>org.java_websocket.client.WebSocketClient</classname>
      <files>
        <zip>https://github.com/liflab/Java-WebSocket/releases/download/v1.3.0/java_websocket-1.3.zip</zip>
      </files>
      <bundle>true</bundle>
    </dependency>
  </dependencies>
</build>
